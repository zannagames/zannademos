//===----------------------------------------------------------------------===//
//
// Part of the Zanna project, under the GNU GPL v3.
// See LICENSE for license information.
//
//===----------------------------------------------------------------------===//
//
// File: games/3dbowling/config.zia
// Purpose: Defines all bounded tuning, state IDs, controls, and save keys for Neon Lanes.
// Key invariants:
//   - State and resource bounds follow LONG_TERM_IMPROVEMENT_SPEC.md.
//   - The implementation remains dependency-free and deterministic where specified.
// Ownership/Lifetime:
//   - Runtime resources are owned by the declaring game subsystem or probe scope.
// Links: LONG_TERM_IMPROVEMENT_SPEC.md, UPGRADE_AUDIT.md
//
//===----------------------------------------------------------------------===//
//
// ============================================================================
// MODULE: config
// PURPOSE: Centralised constants for the 3D Bowling demo. Every tuning
//          knob lives here: window dimensions, camera params, lane
//          geometry, pin layout, ball physics, throw mechanics, lighting,
//          materials, HUD colours, particle counts, action names, save
//          keys, sound IDs.
//
// WHERE-THIS-FITS: Imported by every other module. No state besides
//          `var` definitions (mutable but treated as constants — Zia
//          doesn't yet have const-at-module-scope for compound types).
//
// KEY-DESIGN-CHOICES:
//   * EVERY MAGIC NUMBER NAMED. The renderer, physics setup, and AI
//     should never contain literal numbers; they reference these. This
//     means tuning the demo (faster ball, oilier lane, brighter HUD)
//     happens in one place.
//   * GROUPED BY SUBSYSTEM. Banner comments mark camera / lane / pin /
//     ball / physics / lighting / material / HUD / particles /
//     celebrations / input action names / save / sound. Easy to find
//     the constant you want.
//   * ENUM-LIKE GROUPS. SCREEN_*, STATE_*, MODE_*, QUALITY_*, OIL_*,
//     screen, mode, quality, and oil identifiers are integer ID groups. The naming
//     convention (UPPER_SNAKE prefix) makes them stand out as enum
//     values rather than tuneables.
//   * UNITS COMMENTED INLINE WHERE NON-OBVIOUS. PHYSICS_DT in seconds,
//     SHAKE_PIN_DURATION in seconds, METER_THRESHOLD_MID is fraction
//     0..1.
// ============================================================================

// config.zia — All game constants for 3D Bowling
module config;

// =========================================================================
// App Screens
// =========================================================================
var SCREEN_TITLE: Integer = 0;
var SCREEN_PLAYING: Integer = 1;
var SCREEN_PAUSED: Integer = 2;
var SCREEN_OPTIONS: Integer = 3;
var SCREEN_RESULTS: Integer = 4;

// =========================================================================
// Match Flow States
// =========================================================================
var STATE_AIMING: Integer = 0;
var STATE_ROLLING: Integer = 1;
var STATE_SETTLING: Integer = 2;
var STATE_SCORING: Integer = 3;
var STATE_SWEEPING: Integer = 4;
var STATE_RESETTING: Integer = 5;
var STATE_CELEBRATING: Integer = 6;

// =========================================================================
// Game Modes / Quality
// =========================================================================
var MODE_SINGLE: Integer = 0;
var MODE_VERSUS: Integer = 1;
var MODE_CHAMPIONSHIP: Integer = 2;
var MODE_CHALLENGE: Integer = 3;
var MODE_PRACTICE: Integer = 4;
var MODE_PARTY: Integer = 5;

// Hot-seat player count bounds (1 = single game, 2-4 = versus).
var MAX_PLAYERS: Integer = 4;

var QUALITY_PERFORMANCE: Integer = 0;
var QUALITY_CINEMATIC: Integer = 1;

// Difficulty changes delivery uncertainty and rack variation, never scoring.
var DIFFICULTY_CASUAL: Integer = 0;
var DIFFICULTY_STANDARD: Integer = 1;
var DIFFICULTY_LEAGUE: Integer = 2;

// =========================================================================
// Window
// =========================================================================
var WINDOW_WIDTH: Integer = 1280;
var WINDOW_HEIGHT: Integer = 720;
var WINDOW_TITLE: String = "Neon Lanes Championship";

// =========================================================================
// Camera
// =========================================================================
var CAMERA_FOV: Float = 34.0;
var CAMERA_NEAR: Float = 0.1;
var CAMERA_FAR: Float = 200.0;

var CAM_AIM_HEIGHT: Float = 1.46;
var CAM_AIM_BEHIND: Float = 3.05;
var CAM_AIM_LOOK_Z: Float = 15.15;
var CAM_AIM_LOOK_Y: Float = 0.34;

var CAM_FOLLOW_SMOOTH: Float = 6.6;
var CAM_ROLL_HEIGHT: Float = 1.36;
var CAM_ROLL_BEHIND: Float = 2.92;
var CAM_ROLL_LOOK_Z: Float = 15.65;
var CAM_ROLL_LOOK_Y: Float = 0.31;

var CAM_OVER_HEIGHT: Float = 5.8;

var CAM_CINE_DURATION: Float = 2.6;
var CAM_CINE_HEIGHT: Float = 2.2;
var CAM_CINE_RADIUS: Float = 4.5;
var CAM_CINE_LOOK_Y: Float = 0.35;

var SHAKE_PIN_INTENSITY: Float = 0.08;
var SHAKE_PIN_DURATION: Float = 0.12;
var SHAKE_STRIKE_INTENSITY: Float = 0.22;
var SHAKE_STRIKE_DURATION: Float = 0.35;

// =========================================================================
// Lane Dimensions
// =========================================================================
var LANE_LENGTH: Float = 18.0;
var LANE_WIDTH: Float = 1.06;
var LANE_GUTTER_W: Float = 0.25;
var LANE_WALL_H: Float = 0.15;
var LANE_APPROACH: Float = 4.5;

var LANE_ARROW_Z: Float = 4.5;
var LANE_ARROW_COUNT: Integer = 5;
var LANE_ARROW_SPACING: Float = 0.18;
var LANE_DOT_Z1: Float = 2.0;
var LANE_DOT_Z2: Float = 3.0;
var LANE_DOT_COUNT: Integer = 5;
var LANE_DOT_SPACING: Float = 0.16;
var LANE_BOARD_COUNT: Integer = 39;

var OIL_START_Z: Float = 0.8;
var OIL_END_HOUSE: Float = 12.2;
var OIL_END_SPORT: Float = 9.5;
var OIL_END_DRY: Float = 5.8;
var OIL_FRICTION_OILED: Float = 0.08;
var OIL_FRICTION_BLEND: Float = 0.22;
var OIL_FRICTION_DRY: Float = 0.48;
var OIL_GRID_X: Integer = 13;
var OIL_GRID_Z: Integer = 24;
var OIL_WEAR_MAX: Float = 0.65;
var OIL_CARRY_MAX: Float = 0.30;
var OIL_CARRY_CELLS: Integer = 2;

// Every delivery must reach a terminal state even if physics data is corrupt
// or a body stops making forward progress.
var ROLL_WATCHDOG_SECONDS: Float = 12.0;
var ROLL_PROGRESS_TIMEOUT: Float = 2.0;
var ROLL_PROGRESS_EPSILON: Float = 0.035;
var REPLAY_SAMPLE_STEPS: Integer = 2;
var REPLAY_MAX: Integer = 480;

var OIL_HOUSE: Integer = 0;
var OIL_SPORT: Integer = 1;
var OIL_DRY: Integer = 2;

// =========================================================================
// Pin Layout
// =========================================================================
var PIN_COUNT: Integer = 10;
var PIN_SPACING: Float = 0.305;
var PIN_RADIUS: Float = 0.055;
var PIN_HEIGHT: Float = 0.38;
var PIN_CAPSULE_HEIGHT: Float = PIN_HEIGHT;
var PIN_BODY_RADIUS: Float = PIN_RADIUS * 1.08;
var PIN_BODY_CENTER_Y: Float = PIN_CAPSULE_HEIGHT / 2.0;
var PIN_DRAW_Y_OFFSET: Float = 0.0;
var PIN_MASS: Float = 2.35;
var PIN_RESTITUTION: Float = 0.08;
var PIN_FRICTION: Float = 0.82;
// Lower than the old 0.26/0.72: pins now tumble and slide into neighbors
// instead of resisting the topple, so a good pocket hit carries the rack.
// Angular damping is calibrated for the solver's world-space inertia and
// body-local CCD paths: pins tip about the correct axes while marginal rack
// cascades still leave realistic corner-pin outcomes.
var PIN_LINEAR_DAMPING: Float = 0.34;
var PIN_ANGULAR_DAMPING: Float = 0.76;
var PIN_Z_OFFSET: Float = 15.5;
var PIN_KNOCKDOWN_SPEED: Float = 0.62;
var PIN_KNOCKDOWN_DISPLACEMENT: Float = 0.062;
var PIN_KNOCKDOWN_SPEED_DISPLACEMENT: Float = 0.018;
var PIN_KNOCKDOWN_ANGULAR_SPEED: Float = 1.20;
var PIN_IMPACT_KNOCKDOWN_IMPULSE: Float = 0.36;
var PIN_IMPACT_KNOCKDOWN_SPEED: Float = 0.95;
var PIN_REST_SNAP_DISPLACEMENT: Float = 0.012;
var PIN_SETTLE_SNAP_SPEED: Float = 0.045;
var PIN_TILT_SPEED: Float = 5.8;
var PIN_TILT_MAX: Float = 1.57;
var PIN_DOWN_TILT_COS: Float = 0.72;
var PIN_DOWN_CENTER_Y: Float = 0.145;
var PIN_DOWN_DISPLACEMENT: Float = 0.105;
var PIN_DOWN_DWELL: Float = 0.10;
// Deterministic legal-rack variation is wide enough to preserve corner-pin
// outcomes under body-local CCD. Harder play retains the largest variation.
var RACK_JITTER_CASUAL: Float = 0.0020;
var RACK_JITTER_STANDARD: Float = 0.0055;
var RACK_JITTER_LEAGUE: Float = 0.0070;

var SWEEP_SPEED: Float = 3.0;
var SWEEP_Y_START: Float = 2.0;
var SWEEP_Y_END: Float = 0.1;
var SWEEP_DURATION: Float = 1.05;

var RESET_DURATION: Float = 1.0;
var RESET_DROP_HEIGHT: Float = 2.6;

// =========================================================================
// Ball
// =========================================================================
var BALL_RADIUS: Float = 0.109;
// Runtime sphere/capsule impulses need a gameplay scale below catalog kg so
// pin carry is earned by pocket geometry instead of every head-on collision.
var BALL_MASS_PHYSICS_SCALE: Float = 0.34;
var BALL_RESTITUTION: Float = 0.12;
var BALL_FRICTION: Float = 0.38;
var BALL_SHININESS: Float = 200.0;
var BALL_REFLECTIVITY: Float = 0.42;

var BALL_CATALOG_COUNT: Integer = 6;

// =========================================================================
// Throw Mechanics
// =========================================================================
var THROW_SPEED_MIN: Float = 2.35;
var THROW_POWER_RATE: Float = 0.34;
var THROW_POWER_MAX: Float = 1.0;
var RELEASE_ERROR_X: Float = 0.052;
var RELEASE_ASSIST_OFF: Float = 0.08;
var RELEASE_ASSIST_STANDARD: Float = 0.38;
var RELEASE_ASSIST_FULL: Float = 0.72;
var POWER_NEEDLE_RATE: Float = 0.78;
var ACCURACY_NEEDLE_RATE: Float = 7.35;
var ACCURACY_NEEDLE_PHASE: Float = 1.17;
var RELEASE_CONFIRM_TIMEOUT: Float = 1.10;
var RELEASE_ANGLE_ERROR_MAX: Float = 0.030;
var RELEASE_SPEED_ERROR_MAX: Float = 0.22;
var RELEASE_AXIS_ERROR_MAX: Float = 0.16;
var DIFFICULTY_ERROR_CASUAL: Float = 0.72;
var DIFFICULTY_ERROR_STANDARD: Float = 1.00;
var DIFFICULTY_ERROR_LEAGUE: Float = 1.22;
var STANCE_SPEED: Float = 0.42;
var STANCE_MAX: Float = 0.30;
var TARGET_SPEED: Float = 0.46;
var TARGET_MAX: Float = 0.42;
var SPIN_RATE: Float = 0.48;
var SPIN_MAX: Float = 0.32;
var SPIN_VISUAL_SPEED: Float = 7.0;
var BALL_AXIS_SPIN_SPEED: Float = 2.6;
// Hook forces are what curve the ball into the pocket. The dry backend
// (past oilEndZ) is where the ball "wakes up" and hooks hardest; the oiled
// front skids straight. These were ~25x too weak before (spin was a no-op).
var BACKEND_HOOK_FORCE: Float = 9.0;
var EARLY_HOOK_FORCE: Float = 2.5;
var SPIN_DECAY_OIL: Float = 0.07;
var SPIN_DECAY_DRY: Float = 0.38;
var SIDE_DRAG: Float = 0.40;
var LANE_ASSIST_EDGE_RATIO: Float = 0.34;
var LANE_ASSIST_FORCE: Float = 54.0;
var LANE_ASSIST_DAMP: Float = 4.8;
var LANE_SAFE_HALF_WIDTH: Float = 0.34;
var LANE_STABILIZE_END_Z: Float = PIN_Z_OFFSET - 0.18;
// Gutters are a real failure state: with BUMPERS_ENABLED=0 the ball obeys
// physics and rolls off the lane into the gutter troughs (built in lane.zia).
// BUMPERS_ENABLED=1 restores the old always-on arcade rail (edge assist +
// hard X clamp) so the ball cannot gutter — an optional easy/accessibility mode.
var GUTTER_ENABLED: Integer = 1;
var BUMPERS_ENABLED: Integer = 0;

var BALL_START_X: Float = 0.0;
var BALL_START_Y_OFFSET: Float = 0.026;
var BALL_START_Z: Float = -1.5;
var BALL_THROW_Z: Float = -0.85;
var BALL_TARGET_Z: Float = PIN_Z_OFFSET - 0.95;
// Runtime sphere-floor coupling amplifies lateral velocity during a roll. The
// shared solver and preview both apply this calibrated conversion factor.
var BALL_SIDE_SPEED_SCALE: Float = 0.18;
var BALL_SIDE_SPEED_MAX: Float = 0.62;
// Z just short of the head pin where we sample the ball's entry angle for
// the shot-feedback HUD readout (teaches pocket-angle cause/effect).
var ENTRY_CAPTURE_Z: Float = PIN_Z_OFFSET - 0.6;
// Predicted-curve aim guide: a read-only forward projection of the hook model
// (ball weight + spin + oil pattern), never touches physics. SCALE maps the
// accumulated hook-force shape to lateral world-X displacement.
var HOOK_GUIDE_SEGMENTS: Integer = 22;
var HOOK_GUIDE_SCALE: Float = 0.0135;
// Ball trail: fading ground streak behind a rolling ball (visual only).
var TRAIL_MAX: Integer = 40;
var TRAIL_MIN_SPEED: Float = 0.6;
var TRAIL_FADE: Float = 2.4;
var TRAIL_SAMPLE_SECONDS: Float = 0.024;
var BALL_ROLL_MIN_SPEED: Float = 2.40;
var BALL_DECK_IMPACT_SLOWDOWN: Float = 0.86;
var BALL_DECK_IMPACT_SIDE_DAMP: Float = 0.82;
var CARRY_POWER_FLOOR: Float = 0.44;
var CARRY_POWER_PEAK_END: Float = 0.90;

var GUTTER_Y: Float = -0.11;
var GUTTER_TRIGGER_WIDTH: Float = 0.04;
var GUTTER_EXIT_Z: Float = PIN_Z_OFFSET + 2.8;
var GUTTER_DETECT_Z: Float = 0.80;
var GUTTER_DETECT_END_Z: Float = PIN_Z_OFFSET - 2.80;
var GUTTER_DETECT_OVERHANG: Float = 0.28;

// =========================================================================
// Physics
// =========================================================================
var GRAVITY_Y: Float = -9.81;
var PHYSICS_DT: Float = 0.012;
var DT_CLAMP: Float = 0.05;
var MAX_FIXED_STEPS: Integer = 8;
var SETTLE_TIME: Float = 2.2;
var SETTLE_SPEED_THRESHOLD: Float = 0.065;
var SETTLE_ANGULAR_THRESHOLD: Float = 0.34;
var SETTLE_QUIET_DWELL: Float = 0.28;
var IMPACT_COOLDOWN: Float = 0.08;

var LAYER_STATIC: Integer = 1;
var LAYER_BALL: Integer = 2;
var LAYER_PINS: Integer = 4;
var MASK_BALL: Integer = 5;
var MASK_PINS: Integer = 7;

// =========================================================================
// Lighting
// =========================================================================
var LIGHT_MAIN_DIR_X: Float = -0.2;
var LIGHT_MAIN_DIR_Y: Float = -1.0;
var LIGHT_MAIN_DIR_Z: Float = -0.3;
var LIGHT_MAIN_R: Float = 1.0;
var LIGHT_MAIN_G: Float = 0.98;
var LIGHT_MAIN_B: Float = 0.94;

var LIGHT_FILL_DIR_X: Float = 0.15;
var LIGHT_FILL_DIR_Y: Float = -0.7;
var LIGHT_FILL_DIR_Z: Float = 0.9;
var LIGHT_FILL_R: Float = 0.48;
var LIGHT_FILL_G: Float = 0.56;
var LIGHT_FILL_B: Float = 0.68;

var LIGHT_SPOT_X: Float = 0.0;
var LIGHT_SPOT_Y: Float = 4.8;
var LIGHT_SPOT_Z: Float = 12.8;
var LIGHT_SPOT_R: Float = 1.28;
var LIGHT_SPOT_G: Float = 1.18;
var LIGHT_SPOT_B: Float = 1.06;
var LIGHT_SPOT_ATTENUATION: Float = 0.045;
var LIGHT_SPOT_INNER_ANGLE: Float = 22.0;
var LIGHT_SPOT_OUTER_ANGLE: Float = 42.0;

var AMBIENT_R: Float = 0.07;
var AMBIENT_G: Float = 0.09;
var AMBIENT_B: Float = 0.12;

// =========================================================================
// Materials / Colors
// =========================================================================
var LANE_COLOR_R: Float = 0.34;
var LANE_COLOR_G: Float = 0.18;
var LANE_COLOR_B: Float = 0.055;
var LANE_BOARD_LIGHT_R: Float = 0.48;
var LANE_BOARD_LIGHT_G: Float = 0.27;
var LANE_BOARD_LIGHT_B: Float = 0.075;
var LANE_BOARD_DARK_R: Float = 0.42;
var LANE_BOARD_DARK_G: Float = 0.22;
var LANE_BOARD_DARK_B: Float = 0.050;
var LANE_OIL_COLOR_R: Float = 0.44;
var LANE_OIL_COLOR_G: Float = 0.29;
var LANE_OIL_COLOR_B: Float = 0.12;
var LANE_SHININESS: Float = 96.0;
var LANE_OIL_SHININESS: Float = 180.0;
var LANE_REFLECTIVITY: Float = 0.12;
var LANE_OIL_REFLECTIVITY: Float = 0.42;

var APPROACH_COLOR_R: Float = 0.018;
var APPROACH_COLOR_G: Float = 0.035;
var APPROACH_COLOR_B: Float = 0.070;

var GUTTER_COLOR_R: Float = 0.025;
var GUTTER_COLOR_G: Float = 0.04;
var GUTTER_COLOR_B: Float = 0.07;

var WALL_COLOR_R: Float = 0.008;
var WALL_COLOR_G: Float = 0.020;
var WALL_COLOR_B: Float = 0.045;

var BACKWALL_COLOR_R: Float = 0.003;
var BACKWALL_COLOR_G: Float = 0.010;
var BACKWALL_COLOR_B: Float = 0.025;
var ROOM_FLOOR_R: Float = 0.005;
var ROOM_FLOOR_G: Float = 0.012;
var ROOM_FLOOR_B: Float = 0.028;
var ROOM_SIDE_LANE_R: Float = 0.085;
var ROOM_SIDE_LANE_G: Float = 0.11;
var ROOM_SIDE_LANE_B: Float = 0.14;
var ROOM_SIDE_GUTTER_R: Float = 0.004;
var ROOM_SIDE_GUTTER_G: Float = 0.012;
var ROOM_SIDE_GUTTER_B: Float = 0.030;
var ROOM_WALL_R: Float = 0.004;
var ROOM_WALL_G: Float = 0.014;
var ROOM_WALL_B: Float = 0.038;

var PIN_DECK_COLOR_R: Float = 0.16;
var PIN_DECK_COLOR_G: Float = 0.19;
var PIN_DECK_COLOR_B: Float = 0.22;
var PIN_DECK_SHININESS: Float = 68.0;
var PIN_DECK_REFLECTIVITY: Float = 0.08;

var PIN_COLOR_R: Float = 1.0;
var PIN_COLOR_G: Float = 0.99;
var PIN_COLOR_B: Float = 1.0;
var PIN_SHININESS: Float = 128.0;
var PIN_REFLECTIVITY: Float = 0.12;
var PIN_EMISSIVE: Float = 0.05;
var PIN_NECK_R: Float = 0.96;
var PIN_NECK_G: Float = 0.04;
var PIN_NECK_B: Float = 0.07;
var PIN_NECK_REFLECTIVITY: Float = 0.08;
var PIN_NECK_EMISSIVE: Float = 0.12;

var FOUL_LINE_R: Float = 0.9;
var FOUL_LINE_G: Float = 0.1;
var FOUL_LINE_B: Float = 0.1;
var FOUL_LINE_EMISSIVE: Float = 0.3;

var ARROW_COLOR_R: Float = 0.05;
var ARROW_COLOR_G: Float = 0.18;
var ARROW_COLOR_B: Float = 0.24;

var SWEEP_COLOR_R: Float = 0.32;
var SWEEP_COLOR_G: Float = 0.32;
var SWEEP_COLOR_B: Float = 0.38;

var LIGHT_FIXTURE_R: Float = 0.74;
var LIGHT_FIXTURE_G: Float = 0.68;
var LIGHT_FIXTURE_B: Float = 0.60;

var CLEAR_R: Float = 0.045;
var CLEAR_G: Float = 0.065;
var CLEAR_B: Float = 0.085;

// Shadows + PostFX per quality tier (Plan 06/10 features). Cinematic pushes a
// 2048 cascaded shadow map with Poisson PCF, screen-space reflections on the
// glossy lane, and TAA (bowling's camera is slow, so TAA doesn't ghost);
// Performance drops to a 1024 map, cheaper PCF, and FXAA.
var SHADOW_MAP_CINEMATIC: Integer = 2048;
var SHADOW_MAP_PERFORMANCE: Integer = 1024;
var SHADOW_CASCADES: Integer = 3;
var SHADOW_QUALITY_CINEMATIC: Integer = 2;
var SHADOW_QUALITY_PERFORMANCE: Integer = 1;
var SHADOW_STRENGTH_CINEMATIC: Float = 0.86;
var SHADOW_STRENGTH_PERFORMANCE: Float = 0.72;
var SSR_INTENSITY: Float = 0.42;
var SSR_ROUGHNESS_CUTOFF: Float = 0.35;
var TAA_BLEND: Float = 0.88;
var PARTICLE_SOFTNESS: Float = 1.4;

// =========================================================================
// HUD / Menus
// =========================================================================
var SCORE_Y: Integer = 10;
var SCORE_FRAME_W: Integer = 80;
var SCORE_FRAME_H: Integer = 55;
var SCORE_FRAME_10_W: Integer = 110;

var METER_W: Integer = 160;
var METER_H: Integer = 18;
var METER_COLOR_LOW: Integer = 0x2EDB9B;
var METER_COLOR_MID: Integer = 0xFFD166;
var METER_COLOR_HIGH: Integer = 0xEF476F;
var METER_THRESHOLD_MID: Float = 0.7;
var METER_THRESHOLD_HIGH: Float = 0.9;

var SPIN_INDICATOR_W: Integer = 96;
var SPIN_INDICATOR_H: Integer = 12;

var MSG_DURATION_SHORT: Float = 2.0;
var MSG_DURATION_LONG: Float = 3.5;
var MSG_DURATION_RESULTS: Float = 7.0;

var HUD_COLOR_WHITE: Integer = 0xFFFFFF;
var HUD_COLOR_GRAY: Integer = 0xA8B1BD;
var HUD_COLOR_DARK: Integer = 0x111820;
var HUD_COLOR_GOLD: Integer = 0xFFD166;
var HUD_COLOR_RED: Integer = 0xEF476F;
var HUD_COLOR_GREEN: Integer = 0x2EDB9B;
var HUD_COLOR_CYAN: Integer = 0x56CFE1;
var HUD_COLOR_FRAME_BG: Integer = 0x101820;
var HUD_COLOR_FRAME_ACTIVE: Integer = 0x1D2A34;
var HUD_COLOR_FRAME_BORDER: Integer = 0x4B6472;
var HUD_COLOR_MENU_BG: Integer = 0x0E171D;
var HUD_COLOR_MENU_CARD: Integer = 0x182630;
var HUD_COLOR_MENU_ACCENT: Integer = 0x56CFE1;
var HUD_COLOR_CORAL: Integer = 0xFF6B6B;
var HUD_COLOR_NAVY: Integer = 0x0B1B2B;
var HUD_COLOR_AMBER: Integer = 0xFFB547;

// =========================================================================
// Particles / Decals
// =========================================================================
var SPARK_COUNT: Integer = 24;
var SPARK_SPEED_MIN: Float = 2.0;
var SPARK_SPEED_MAX: Float = 6.0;
var SPARK_LIFETIME_MIN: Float = 0.12;
var SPARK_LIFETIME_MAX: Float = 0.42;
var SPARK_SIZE_START: Float = 0.05;
var SPARK_SIZE_END: Float = 0.01;
var SPARK_COLOR_START: Integer = 0xFFEECC;
var SPARK_COLOR_END: Integer = 0xFF8844;

var FIREWORK_COUNT: Integer = 80;
var FIREWORK_SPEED_MIN: Float = 3.0;
var FIREWORK_SPEED_MAX: Float = 10.0;
var FIREWORK_LIFETIME_MIN: Float = 0.5;
var FIREWORK_LIFETIME_MAX: Float = 1.5;
var FIREWORK_SIZE_START: Float = 0.08;
var FIREWORK_SIZE_END: Float = 0.02;

var DUST_RATE: Float = 18.0;
var DUST_SPEED_MIN: Float = 0.15;
var DUST_SPEED_MAX: Float = 0.8;
var DUST_LIFETIME_MIN: Float = 0.25;
var DUST_LIFETIME_MAX: Float = 0.7;
var DUST_SIZE_START: Float = 0.03;
var DUST_SIZE_END: Float = 0.07;
var DUST_COLOR: Integer = 0xCCBB99;

var DECAL_MAX: Integer = 48;
var DECAL_LIFETIME: Float = 8.0;

// =========================================================================
// Celebrations / Scores
// =========================================================================
var CELEBRATE_STRIKE_DURATION: Float = 2.5;
var CELEBRATE_SPARE_DURATION: Float = 1.6;
var CELEBRATE_TURKEY_DURATION: Float = 3.0;
var CELEBRATE_GUTTER_DURATION: Float = 1.5;
var CELEBRATE_PERFECT_DURATION: Float = 5.0;
var CELEBRATE_FLASH_DURATION: Float = 0.3;
var CELEBRATE_FLASH_ALPHA: Float = 0.4;

var FRAMES_PER_GAME: Integer = 10;
var MAX_SCORE: Integer = 300;

// =========================================================================
// Input Action Names
// =========================================================================
var ACT_MENU_UP: String = "menu_up";
var ACT_MENU_DOWN: String = "menu_down";
var ACT_CONFIRM: String = "confirm";
var ACT_BACK: String = "back";
var ACT_STANCE_X: String = "stance_x";
var ACT_TARGET_X: String = "target_x";
var ACT_SPIN_X: String = "spin_x";
var ACT_THROW: String = "throw";
var ACT_SPARE_RELEASE: String = "spare_release";
var ACT_BALL_PREV: String = "ball_prev";
var ACT_BALL_NEXT: String = "ball_next";
var ACT_RESET: String = "reset";
var ACT_PAUSE: String = "pause";
var ACT_CAMERA: String = "camera_cycle";

// =========================================================================
// Save Keys
// =========================================================================
var SAVE_NAME: String = "3dbowling";
var SAVE_VERSION: Integer = 4;
var SAVE_KEY_BEST_SCORE: String = "best_score";
var SAVE_KEY_GAMES_PLAYED: String = "games_played";
var SAVE_KEY_OIL_PATTERN: String = "oil_pattern";
var SAVE_KEY_QUALITY: String = "quality";
var SAVE_KEY_LAST_MODE: String = "last_mode";
var SAVE_KEY_SETTINGS_VERSION: String = "settings_version";
var SAVE_KEY_PLAYER_COUNT: String = "player_count";
var SAVE_KEY_MASTER_VOLUME: String = "master_volume";
var SAVE_KEY_MUSIC_VOLUME: String = "music_volume";
var SAVE_KEY_EFFECTS_VOLUME: String = "effects_volume";
var SAVE_KEY_CAMERA_SHAKE: String = "camera_shake";
var SAVE_KEY_REPLAY_MODE: String = "replay_mode";
var SAVE_KEY_HIGH_CONTRAST: String = "high_contrast";
var SAVE_KEY_ASSIST_LEVEL: String = "assist_level";
var SAVE_KEY_DIFFICULTY: String = "difficulty";
var SAVE_KEY_BUMPERS: String = "bumpers";
var SAVE_KEY_REDUCED_MOTION: String = "reduced_motion";
var SAVE_KEY_REDUCED_FLASH: String = "reduced_flash";
var SAVE_KEY_LARGE_HUD: String = "large_hud";
var SAVE_KEY_CONTROL_PRESET: String = "control_preset";
var SAVE_KEY_SELECTED_BALL: String = "selected_ball";
var SAVE_KEY_CHALLENGE_MEDALS: String = "challenge_medals";
var SAVE_KEY_CAREER_POINTS: String = "career_points";
var SAVE_KEY_ACHIEVEMENT_MASK: String = "achievement_mask";
var SAVE_KEY_TOTAL_STRIKES: String = "total_strikes";
var SAVE_KEY_TOTAL_SPARES: String = "total_spares";
var SAVE_KEY_TOTAL_GUTTERS: String = "total_gutters";
var SAVE_KEY_TOTAL_PINS: String = "total_pins";
