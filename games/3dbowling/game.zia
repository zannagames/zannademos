// ============================================================================
// MODULE: game (BowlingGame)
// PURPOSE: Thin 3D Bowling orchestrator.
//
// WHERE-THIS-FITS: Constructed by main.zia. The implementation is split across
//          engine/game_*.zia so each subsystem-sized layer stays readable.
// ============================================================================

// game.zia — Public BowlingGame leaf class.
module game;

bind "./engine/game_render";

/// @brief Demo entry class. Behavior lives in the engine/game_* layers.
class BowlingGame extends BowlingGameRender {
    override expose func init() {
        super.init();
    }
}
